# Flyingnets 官网项目规则

## 部署方式：OrbStack（优先）
用户使用 OrbStack 部署，以后也以 OrbStack 为主。
- 启动：`npm run orbstack` 或 `docker compose up -d --build`
- 访问：http://flyingnets.hp.orb.local 或 http://localhost:3000
- 若无法打开：见 PREVIEW.md 故障排查

## 更新后确保可访问
每次代码更新后，用户通过 OrbStack 启动。Docker 构建使用 DOCKER_BUILD=1 启用 standalone 输出。
